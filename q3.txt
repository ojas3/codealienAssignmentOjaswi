#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int a=3;
	int d=2;
	int n=0;
	cout<<"Enter number of persons: ";
	cin>>n;
	int pow2=0;
	for(int i=0;i<n;i++){
	    if ((pow(2,i)<n)&&pow(2,(i+1))>n)
	    pow2=pow(2,i);
	}
	int x=a+d*(n-pow2-1);
	cout<<x;
	return 0;
}